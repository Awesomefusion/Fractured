export interface Skill {
        id: string;
        title: string;
        school: string;
        description: string;
        requirements: string;
        cooldown: string;
        image: string;
    }